.form-row {
    @apply flex flex-col mb-4;

    label {
        @apply mb-2 uppercase font-bold text-lg text-white;
    }

    input {
        @apply py-2 px-3 text-white bg-transparent text-lg border-b-4 border-white font-bold;
    }
}
